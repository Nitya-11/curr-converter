const BASE_URL = "https://api.currencyapi.com/v3/latest?apikey=cur_live_SS60ndNPYbRKvT3nOE0a4DGicyxRWSbWXdbWU1PE";

const dropdowns = document.querySelectorAll(".dropdown select");

for (let select of dropdown) {
    for(currCode in countryList) {
        let newOption = 
    }
}